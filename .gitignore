# Local IDE / assistant artifacts (do not publish)
.specstory/

# Local secrets (e.g. GEMINI_API_KEY for `python -m agent serve`)
.env
.env.ebpf
.gemini_api_key
.github_token
.new_relic_license_key
.clickhouse_endpoint
.clickhouse_user
.clickhouse_password

.venv/
examples/openai_otel/.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
dist/otel-custom/
dist/otelcol-contrib/

# Local backend configs may contain secrets if edited — keep examples only in git
collector/collector-config-nr.yaml
collector/collector-config-dual.yaml
collector/collector-config-clickhouse.yaml
