# Optional venv: python -m venv .venv && .venv\Scripts\activate && pip install -r requirements.txt
# Traceloop's google-generativeai instrumentation targets the CLASSIC google-generativeai
# SDK (import `google.generativeai`), NOT the newer `google-genai` SDK.
# Version stack matches the anthropic example (opentelemetry-api>=1.38, still <2).
google-generativeai>=0.8.0,<1
# Uses the OS trust store (Windows) so corporate TLS proxies don't break HTTPS.
truststore>=0.9.0
opentelemetry-api>=1.38.0,<2
opentelemetry-sdk>=1.38.0,<2
opentelemetry-exporter-otlp-proto-http>=1.38.0,<2
opentelemetry-instrumentation-google-generativeai>=0.61.0,<1
